<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="mail_template_rma_portal_approved" model="mail.template">
        <field name="name">RMA Portal – Anfrage genehmigt</field>
        <field name="model_id" ref="model_rma_portal_request"/>
        <field name="subject">Ihre RMA-Anfrage {{ object.name }} wurde genehmigt</field>
        <field name="email_to">{{ object.partner_id.email }}</field>
        <field name="body_html"><![CDATA[
<div style="font-family: Arial, sans-serif; max-width: 600px; margin: 0 auto;">
    <div style="background: #714b67; padding: 24px 32px; border-radius: 8px 8px 0 0;">
        <h2 style="color: #fff; margin: 0; font-size: 1.3rem;">RMA-Rücksendung genehmigt</h2>
    </div>
    <div style="background: #fff; border: 1px solid #e0e0e0; border-top: none; padding: 28px 32px; border-radius: 0 0 8px 8px;">
        <p>Guten Tag {{ object.partner_id.name }},</p>
        <p>Ihre RMA-Anfrage wurde geprüft und genehmigt. Sie können die Ware nun an uns zurücksenden.</p>

        <div style="background: #f5f6f8; border-radius: 8px; padding: 16px 20px; margin: 20px 0;">
            <table style="width: 100%; border-collapse: collapse;">
                <tr>
                    <td style="color: #888; padding: 4px 0; width: 40%;">Anfrage-Nr.</td>
                    <td style="font-weight: bold;">{{ object.name }}</td>
                </tr>
                <tr>
                    <td style="color: #888; padding: 4px 0;">Rechnung</td>
                    <td>{{ object.invoice_id.name }}</td>
                </tr>
                <tr>
                    <td style="color: #888; padding: 4px 0;">Rückgabegrund</td>
                    <td>{{ object.rma_reason_id.name }}</td>
                </tr>
            </table>
        </div>

        <h3 style="font-size: 1rem; margin-top: 24px;">Zurückzusendende Artikel</h3>
        <table style="width: 100%; border-collapse: collapse; font-size: 0.9rem;">
            <thead>
                <tr style="background: #f0eaf3;">
                    <th style="text-align: left; padding: 8px 12px; border-radius: 4px 0 0 4px;">Artikel</th>
                    <th style="text-align: right; padding: 8px 12px; border-radius: 0 4px 4px 0;">Menge</th>
                </tr>
            </thead>
            <tbody>
                % for line in object.line_ids:
                <tr style="border-bottom: 1px solid #eee;">
                    <td style="padding: 8px 12px;">{{ line.product_id.name }}</td>
                    <td style="padding: 8px 12px; text-align: right;">{{ line.qty_requested }}</td>
                </tr>
                % endfor
            </tbody>
        </table>

        <div style="background: #fff3cd; border: 1px solid #ffc107; border-radius: 6px; padding: 14px 18px; margin: 24px 0;">
            <strong>Bitte beachten:</strong> Bitte schreiben Sie die Anfrage-Nr.
            <strong>{{ object.name }}</strong> gut sichtbar auf das Paket oder legen Sie
            dieses Schreiben bei.
        </div>

        <h3 style="font-size: 1rem;">Rücksendeadresse</h3>
        <p style="margin: 0;">
            {{ user.company_id.name }}<br/>
            % if user.company_id.street:
            {{ user.company_id.street }}<br/>
            % endif
            % if user.company_id.zip or user.company_id.city:
            {{ user.company_id.zip }} {{ user.company_id.city }}<br/>
            % endif
        </p>

        <p style="margin-top: 24px;">Bei Fragen stehen wir Ihnen gerne zur Verfügung.</p>
        <p>Mit freundlichen Grüßen<br/>{{ user.company_id.name }}</p>
    </div>
</div>
        ]]></field>
        <field name="lang">{{ object.partner_id.lang }}</field>
        <field name="auto_delete" eval="True"/>
    </record>
</odoo>
